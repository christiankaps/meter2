name: Build and Attach Release

on:
  release:
    types: [published]

permissions:
  contents: write

jobs:
  build:
    name: Archive and package DMG
    runs-on: macos-26
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Select Xcode 26.4
        run: sudo xcode-select -s /Applications/Xcode_26.4.app/Contents/Developer

      - name: Print Xcode version
        run: xcodebuild -version

      - name: Derive release version
        run: |
          RAW_TAG="${{ github.event.release.tag_name }}"
          VERSION="${RAW_TAG#v}"
          VERSION="${VERSION#V}"

          if [ -z "$VERSION" ]; then
            echo "Release tag did not contain a version"
            exit 1
          fi

          echo "RELEASE_VERSION=$VERSION" >> "$GITHUB_ENV"
          echo "BUILD_NUMBER=${{ github.run_number }}" >> "$GITHUB_ENV"

      - name: Archive
        run: |
          xcodebuild \
            -project Meter2.xcodeproj \
            -scheme Meter2 \
            -configuration Release \
            -archivePath "$RUNNER_TEMP/Meter2.xcarchive" \
            archive \
            MARKETING_VERSION="$RELEASE_VERSION" \
            CURRENT_PROJECT_VERSION="$BUILD_NUMBER" \
            SKIP_INSTALL=NO \
            BUILD_LIBRARY_FOR_DISTRIBUTION=NO \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO

      - name: Verify archive
        run: |
          APP_PATH="$RUNNER_TEMP/Meter2.xcarchive/Products/Applications/Meter2.app"
          [ -d "$APP_PATH" ] || { echo "Archive did not produce Meter2.app"; exit 1; }

      - name: Install create-dmg
        run: brew install create-dmg

      - name: Create DMG
        run: |
          APP_PATH="$RUNNER_TEMP/Meter2.xcarchive/Products/Applications/Meter2.app"
          VERSION=$(/usr/libexec/PlistBuddy -c "Print CFBundleShortVersionString" "$APP_PATH/Contents/Info.plist" 2>/dev/null || echo "0.0.0")
          BUILD=$(/usr/libexec/PlistBuddy -c "Print CFBundleVersion" "$APP_PATH/Contents/Info.plist" 2>/dev/null || echo "1")
          DMG_NAME="Meter2_${VERSION}_${BUILD}.dmg"

          STAGING="$RUNNER_TEMP/staging"
          mkdir -p "$STAGING"
          cp -R "$APP_PATH" "$STAGING/"

          create-dmg \
            --volname "Meter2" \
            --window-pos 200 120 \
            --window-size 660 400 \
            --icon-size 128 \
            --icon "Meter2.app" 180 170 \
            --app-drop-link 480 170 \
            --hide-extension "Meter2.app" \
            --no-internet-enable \
            "$RUNNER_TEMP/$DMG_NAME" \
            "$STAGING"

          [ -f "$RUNNER_TEMP/$DMG_NAME" ] || { echo "DMG was not created"; exit 1; }
          echo "DMG_PATH=$RUNNER_TEMP/$DMG_NAME" >> "$GITHUB_ENV"
          echo "DMG_NAME=$DMG_NAME" >> "$GITHUB_ENV"

      - name: Upload DMG to release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release upload "${{ github.event.release.tag_name }}" \
            "$DMG_PATH#$DMG_NAME" \
            --clobber
